name: marketplace_mobile
description: Flutter mobile app for the Iraqi Marketplace platform.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: 6.1.5+1
  shared_preferences: ^2.2.2
  flutter_dotenv: ^5.1.0
  intl: ^0.20.0
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
